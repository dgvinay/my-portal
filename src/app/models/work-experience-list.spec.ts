import { WorkExperienceList } from './work-experience-list';

describe('WorkExperienceList', () => {
  it('should create an instance', () => {
    expect(new WorkExperienceList()).toBeTruthy();
  });
});
